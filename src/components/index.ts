// src/components — reusable, presentational UI primitives.
//
// These are the small building blocks used across MANY screens: Button, Text,
// Input, Card, Screen, etc. They should be "dumb": they take props and render,
// with no knowledge of navigation, servers, or business rules. That keeps them
// reusable and easy to test.
//
// Module 6 fills this folder. For now it's an empty barrel so `@/components`
// already resolves and git tracks the folder.
//
// When you add a component, create `src/components/Button.tsx` (default export)
// and re-export it here:
//   export { default as Button } from './Button';

export {};

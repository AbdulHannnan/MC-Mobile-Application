// src/hooks — shared custom React hooks used across features.
//
// A custom hook is a reusable function whose name starts with `use` and that
// calls other hooks (useState, useEffect, etc.). Put GENERIC hooks here
// (e.g. useDebounce, useKeyboardVisible). Hooks that belong to a single
// feature live inside that feature folder instead (src/features/<name>/hooks).
//
// Empty barrel for now. Add hooks like:
//   export { useDebounce } from './useDebounce';

export {};

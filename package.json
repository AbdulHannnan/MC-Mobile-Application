{
  "name": "microcare",
  "version": "1.0.0",
  "private": true,
  "// main": "Entry point. Unlike a web React app (index.html + ReactDOM.render), a native app boots through Expo's AppEntry, which calls registerRootComponent(App) for us. It auto-loads ./App.tsx from the project root.",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "// start": "Starts the Metro bundler + Expo dev server. You then open the app in Expo Go (scan the QR) or an emulator.",
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  },
  "// dependencies": "Versions are pinned to Expo SDK 54 so every package is mutually compatible. '~' means 'patch updates allowed'. React Native's version is chosen by the SDK, not by us.",
  "dependencies": {
    "babel-preset-expo": "~54.0.10",
    "expo": "^54.0.0",
    "expo-asset": "~12.0.13",
    "expo-constants": "~18.0.14",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/react": "~19.1.0",
    "babel-plugin-module-resolver": "^5.0.3",
    "typescript": "^5.3.3"
  }
}
